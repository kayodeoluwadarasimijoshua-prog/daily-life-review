# Optional — omit everything to use the built-in local analysis engine.
# Weekly reviews work out-of-the-box with zero configuration.

# AI_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
